#include "main.h"
/**
 * _abs - print the absolute value of an integer
 * @num: the number checked
 *
 * Return: the absolute value of num
 */
int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
