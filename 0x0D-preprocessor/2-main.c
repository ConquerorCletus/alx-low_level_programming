#include <stdio.h>

/**
 * main - Program prints the name of the program file
 *        was compiled from, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
